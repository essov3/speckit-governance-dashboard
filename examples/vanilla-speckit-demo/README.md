# Vanilla SpecKit demo

This is a synthetic, clean demo used by the README and CI. Run it from the dashboard repository:

```bash
npm run dashboard:generate -- --project-root ./examples/vanilla-speckit-demo --deterministic
```
